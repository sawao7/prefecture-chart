import React from "react";
import styles from "@/components/atoms/MainTitle/styles.module.scss";

const MainTitle: React.FC = () => {
  return <p className={styles.title_mini}>都道府県</p>;
};

export default MainTitle;
